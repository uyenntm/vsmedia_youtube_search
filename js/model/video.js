class Video {
    constructor(index, id,title, date, viewCount ){
        this.index =index;
        this.id = id; 
        this.title = title;
        this.date = date;
        this.viewCount = Number(viewCount);
    }
}